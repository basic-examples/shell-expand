{
  "private": true,
  "name": "shell-expand",
  "version": "0.0.1",
  "description": "Expand shell-style variable substitution",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/shell-expand.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && cpy README.md dist && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
